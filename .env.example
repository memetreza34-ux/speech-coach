# Server-seitiger Gemini-Key für /api/analyze.js.
# Lokal: Datei nach .env.local kopieren und mit `vercel dev` starten.
# Auf Vercel: als Environment Variable im Projekt-Dashboard setzen (nicht committen!).
GEMINI_API_KEY=
